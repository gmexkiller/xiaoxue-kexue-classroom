# 小学科学交互教学平台开发规范

## 项目目标

开发真正服务小学科学课堂的交互教学工具，而不是单纯好看的网页或功能堆叠型软件。

始终遵循：

- 教学需要决定功能
- 科学内容决定模型
- 课堂使用决定界面
- 科学准确 > 真实实验 > 学生思考 > 教师操作 > 视觉效果 > 功能数量

---

## 开发前必须确认

每个新的课程任务，不得收到资料后立即开始正式编码。

首先读取课程文件夹中的教材、教案、PPT、PDF、图片、实验方案等资料。

然后先向用户输出一份：

# 《教学与开发确认单》

至少包括：

1. 核心科学概念
2. 核心学习目标
3. 平台主要服务的课堂环节
4. 最核心的一个交互
5. 学生通过该交互能够发现或训练什么
6. 真实实验与数字平台的分工
7. 建议课堂流程
8. 拟实现主要功能
9. 科学红线
10. 真正需要用户确认的问题

此阶段不得开始制作最终 HTML。

等待用户确认后再进入正式开发。

---

## 教学原则

- 真实实验优先。
- 数字平台不得替代适合学生亲手完成的真实实验。
- 学生应先观察、预测、表达，再揭晓结论。
- 不得制造没有真实数据或教材依据的伪精确模拟。
- 不为了显得高级加入无关动画、积分、排行榜、复杂3D或过度游戏化功能。
- 每个主要页面尽量只解决一个核心教学问题。
- 科学模型必须明确属于简化模型，不能表现成真实过程录像。
- 平台必须服务教师组织课堂，而不是替代教师讲课。

---

## 科学要求

开发过程中必须检查：

- 教材术语
- 科学概念
- 单位
- 数值范围
- 变量关系
- 因果关系
- 实验条件
- 实验装置
- 科学模型
- 结论适用范围

科学内容存在不确定性时不得自行编造。

---

## 技术要求

默认最终交付：

- 单文件 HTML
- HTML、CSS、JavaScript 内联
- 双击可以离线运行
- 不依赖 CDN
- 不依赖在线字体
- 不依赖在线图片
- 不依赖在线 API
- 不依赖服务器

主要支持：

- 鼠标
- 触摸屏

核心科学变量必须统一管理。

数字、图形、动画、文字、记录和结论必须保持同步。

---

## 界面要求

优先适配：

- 1366×768
- 1440×900
- 1920×1080

核心课堂页面应：

- 后排可看清
- 按钮容易找到
- 核心现象突出
- 尽量不依赖滚动完成主要操作
- 不堆积大量文字和卡片
- 教师一眼知道当前要让学生看什么、想什么、做什么

---

## 开发流程

用户确认《教学与开发确认单》后：

设计  
→ 编码  
→ 本地运行  
→ 浏览器检查  
→ 科学检查  
→ 交互检查  
→ 课堂流程检查  
→ 修复  
→ 再测试  
→ 最终交付

不得只生成代码而不实际检查。

---

## 最终验收

至少确认：

- 页面可以正常打开
- 无影响运行的 JavaScript 报错
- 主要按钮正常
- 拖动、点击、重置等交互正常
- 状态同步
- 最小值、最大值和典型情况正常
- 目标分辨率不存在严重遮挡和裁切
- 没有明显无教学价值的功能

如果某个功能只是为了“显得高级”，优先删除。

---

## Reference 使用规则

`reference/` 中的文件是长期通用规范，不要求每次任务都完整读取。

默认优先读取当前课程：

`lessons/对应课程/source/`

中的教材、教案、课件和其他资料。

只有在任务确实涉及以下内容时，才按需查阅 reference：

- 涉及课程目标、学段要求、核心素养、课程内容边界：  
  查阅 `reference/义务教育科学课程标准（2022年版）.pdf`

- 涉及实验操作、实验器材、实验室使用或安全：  
  查阅 `reference/《中小学实验室规程》.docx`

不要为了形式完整阅读所有 reference 文件。

优先采用“查找相关章节 / 关键词 / 条款”的方式，而不是整份文件全文读取。

如果本次任务已经读取过相关 reference 内容，后续优先复用已获得的信息，不重复全文读取。

如当前课程资料已经足以支持教学设计，不必额外读取 reference。

## UI 与 3D 设计规范

- 后续课程开发应参考 `reference-ui/DESIGN_REFERENCE.md` 的设计语言，但具体功能和 3D 模拟必须由当前课程教学目标决定。
- 当课程需要空间观察、器材结构、遮挡关系或多角度操作时，优先采用真实 WebGL / Three.js 场景，不得为了方便把核心 3D 效果简化成普通二维 SVG 或 CSS 动画。
- 3D 场景应统一管理相机、光照、材质、动画、交互变量和教学状态；数字、图形、动画、文字、记录和结论必须保持同步。
- 优先使用本地固定版本的 3D 运行库和本地资源，禁止依赖 CDN、在线字体、在线图片、在线模型或在线 API；高质量 3D 课程可以使用维护态的 `js/`、`models/`、`textures/`、`assets/`、`vendor/` 目录。
- 普通二维课程仍可交付为单文件 HTML；是否采用高质量 3D 目录结构，应在当前课程教学确认后决定。
- 透明材质、环境贴图、阴影、粒子、流线和 shader 只在能帮助学生观察或操作时使用，并明确模型是实测、真实实验还是简化示意。

## 3D 素材使用与下载规则

- 不提前批量下载大量 3D 模型、纹理、HDRI 或其他素材。
- 每开发一节新课程时，先根据教学目标和场景需求判断：
  - 哪些对象可以直接使用现有 `assets/` 中的本地资源；
  - 哪些对象适合使用 Three.js 程序化建模；
  - 哪些对象确实需要外部 3D 模型；
  - 哪些场景需要 PBR 纹理；
  - 哪些场景需要 HDRI；
  - 是否真的需要新增素材。
- 3D 素材使用优先级统一为：现有本地资源 → 程序化建模 → 按需下载且授权清晰的新资源。
- 简单规则化器材优先程序化建模，例如烧杯、试管、管道、实验箱、斜面、杠杆、滑轮、简单支架、基础容器和简单几何实验器材。不要为了使用外部模型而使用外部模型。
- 以下类型如果程序化建模明显影响真实感或开发效率，可以按需下载：山地、岩石、树木、自然地形、动植物、人体结构、复杂科学仪器和高细节环境对象。
- 外部素材优先选择 CC0、Public Domain 或其他明确允许当前使用方式的资源。不使用授权不明确的素材。
- 下载新素材前，优先从当前已认可来源中寻找，例如 Poly Haven、ambientCG、Kenney 以及授权明确的 Sketchfab 资源。
- 新增任何外部素材后，必须同步记录到 `assets/licenses/ASSET_LICENSES.md`，至少记录：文件名、来源网站、原资源页面、作者、License、是否要求署名、下载日期和使用目的。
- 所有正式课堂使用的模型、纹理和 HDRI 必须本地化保存。课程运行时不得依赖外部 CDN、在线模型地址、在线纹理地址或运行时网络下载。
- 素材质量以课堂大屏效果和稳定性能为目标，不追求影视级资源。优先使用 GLB / glTF、低到中等面数、1K～2K 纹理和合理大小的 HDRI。
- 谨慎使用超高面数模型、4K / 8K 大量纹理、超大 HDRI、大量透明材质、高成本后处理和大规模粒子系统。
- 不要为了“丰富页面”而堆积 3D 模型、纹理或特效。任何 3D 素材都必须服务于科学观察、实验操作、空间理解、结构理解、现象解释或教学问题。
- 以后开发新课程时，如果发现需要新增素材，应先在《教学与开发确认单》中说明：需要什么素材、为什么需要、是否已有替代资源、程序化建模是否可行、是否需要外部下载。确认后再下载或新增。
